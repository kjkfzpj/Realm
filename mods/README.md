# Mods

Drop folders here to add content to City Builder. Each mod folder must contain
a `manifest.json` and may contain `zones/`, `buildings/`, and a `balance.json`.

See `docs/modding-guide.md` for the full format and load order rules.

A minimal example lives in `mods/example-mod/`.
